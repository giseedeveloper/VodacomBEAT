---------------------------------------------
Install JDK 17
---------------------------------------------
> sudo apt-get update;
> sudo apt-get install openjdk-17-jdk

sudo apt install mysql-server

hardware@home